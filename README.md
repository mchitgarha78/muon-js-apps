Muon App Runner JS is a server that run a js Muon app and returns hash of its result

## Configure

```
cp .env.example .env
```

## Run

Copy your MuonApps to `muon-apps` directoy.

Run the server using:

```
npm start
```
